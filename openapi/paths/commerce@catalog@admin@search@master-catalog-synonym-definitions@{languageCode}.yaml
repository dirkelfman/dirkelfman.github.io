get:
  tags:
    - Search
  parameters:
    - name: languageCode
      in: path
      required: true
      schema:
        type: string
        nullable: true
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: ../components/schemas/SynonymDefinitionCollection.yaml
        text/json:
          schema:
            $ref: ../components/schemas/SynonymDefinitionCollection.yaml
        text/plain:
          schema:
            $ref: ../components/schemas/SynonymDefinitionCollection.yaml
