get:
  tags:
    - ProductSearch
  parameters:
    - name: siteId
      in: path
      required: true
      schema:
        type: integer
        format: int32
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/SearchTuningRuleSolr.yaml
        text/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/SearchTuningRuleSolr.yaml
        text/plain:
          schema:
            type: array
            items:
              $ref: ../components/schemas/SearchTuningRuleSolr.yaml
