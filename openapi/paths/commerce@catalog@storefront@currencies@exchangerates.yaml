get:
  tags:
    - CurrencyRuntime
  parameters:
    - name: x-vol-tenant
      in: header
      schema:
        type: integer
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/CurrencyExchangeRate.yaml
        text/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/CurrencyExchangeRate.yaml
        text/plain:
          schema:
            type: array
            items:
              $ref: ../components/schemas/CurrencyExchangeRate.yaml
